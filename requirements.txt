# ── Cash Guard — Bağımlılıklar ─────────────────────────────────────────────
# Çekirdek (zorunlu). Sürümler alt sınır olarak verildi; daha yenisi de çalışır.
streamlit>=1.49    # width="stretch" API'si (use_container_width 2025 sonunda kaldırıldı)
pandas>=2.2
numpy>=1.26
plotly>=5.22
reportlab>=4.0     # yönetim kuruluna sunulacak PDF rapor üretimi

# ── Yapay Zeka "Acımasız CFO" ajanı ───────────────────────────────────────
# SDK KURULU DEĞİLSE uygulama yine çalışır: kural tabanlı (rule-based) yerel
# CFO motoruna otomatik düşer, API anahtarı gerekmez.
#
# Claude SDK'sı açık, çünkü Streamlit Cloud YALNIZCA bu dosyayı kurar: paket
# yorumlu kalırsa Secrets'a ANTHROPIC_API_KEY yazmak hiçbir şey değiştirmez,
# uygulama sessizce kural tabanlı motorda kalır. Anahtar yoksa da zararı yok —
# paket kurulur, kullanılmaz, fallback devrededir.
anthropic>=0.40                # -> ANTHROPIC_API_KEY (en iyi metin, kredi ister)

# Gemini de açık, çünkü ücretsiz katmanı var: aistudio.google.com'dan kart
# vermeden anahtar alınıp GOOGLE_API_KEY olarak Secrets'a konabiliyor. Claude
# kredi ister; bu, demoyu gerçek bir LLM'le koşturmanın parasız yolu.
google-generativeai>=0.7       # -> GOOGLE_API_KEY (veya GEMINI_API_KEY)

# Birden fazla anahtar tanımlıysa deneme sırası: Claude -> OpenAI -> Gemini.
# Model adları CG_CLAUDE_MODEL / CG_OPENAI_MODEL / CG_GEMINI_MODEL ile ezilir.
# openai>=1.30                 -> OPENAI_API_KEY

# ── Opsiyonel: Test ───────────────────────────────────────────────────────
# Testler pytest OLMADAN da koşar (her dosyada __main__ bloğu var):
#   python tests/test_finance_math.py
# Toplu ve renkli çıktı isteyen için:
#   pip install pytest && python -m pytest tests/ -q
# pytest>=8.0

# ── Opsiyonel: Performans hızlandırma ─────────────────────────────────────
# Monte Carlo çekirdeği saf NumPy ile vektörize edilmiştir ve numba OLMADAN
# da hızlıdır; numba kuruluysa sıcak döngü JIT ile derlenir.
#
# Ölçüm (20.000 senaryo x 24 ay, Windows / Python 3.14 / numba 0.66):
#   NumPy vektörize          : 9.4 ms
#   Numba, çağrı başına      : 1.6 ms      (~6x)
#   Numba, ilk derleme       : 3.6 s       (bir kez; cache=True diske yazar)
#   Numba, sonraki process   : ~0.5 s      (cache'ten yükleme)
# Yani numba tek atışlık kullanımda ZARARLI, sürgü oynatılan canlı arayüzde
# kazançlı — Streamlit aynı process'te yeniden çalıştığı için ikinci durumdayız.
# Yorumlu bırakıldı çünkü uygulama numba'sız tam işlevli; isteyen açsın.
# numba>=0.60           # opsiyonel — 0.66 ile Python 3.14 tekerleği mevcut
